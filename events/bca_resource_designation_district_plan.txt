namespace = colony_automation_event

carrier_event = {
	id = colony_automation_event.10
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		remove_carrier_flag = bca_pf_advance_controller_order

		bca_resource_planet_controller_district_only = yes

		if = {
			limit = {
				free_district_slots = 0 #replace district
				check_variable = {
					which = bca_num_queued_districts
					value = 0
				}
				NOR = {
					has_carrier_flag = bca_pf_disable_planet_auto_destruction_district
				}
				bca_auto_district_manage_enabled = yes
			}
			if = { #check by year
				limit = {
					has_carrier_flag = bca_pf_district_balanced
				}
				add_variable = {
					which = bca_district_balanced_test_counter
					value = 1
				}
				if = {
					limit = {
						check_variable = {
							which = bca_district_balanced_test_counter
							value >= 12
						}
					}
					# bca_test_primary_and_secondary_district_bidirectional_replace = yes
					bca_resource_district_replace_controller = yes
					set_variable = {
						which = bca_district_balanced_test_counter
						value = 0
					}
				}
			}
			else = { #check by month
				if = {
					limit = {
						NOT = { has_carrier_flag = bca_pf_has_district_remove_plan }
					}
					set_carrier_flag = bca_pf_previous_has_district_remove_plan_clear
				}

				# bca_test_primary_and_secondary_district_bidirectional_replace = yes
				bca_resource_district_replace_controller = yes

				if = {
					limit = {
						has_carrier_flag = bca_pf_previous_has_district_remove_plan_clear
						NOT = { has_carrier_flag = bca_pf_has_district_remove_plan }
					}
					set_carrier_flag = bca_pf_district_balanced
				}
				remove_carrier_flag = bca_pf_previous_has_district_remove_plan_clear
			}
		}
		else = {
			remove_carrier_flag = bca_pf_district_balanced
			clear_variable = bca_district_balanced_test_counter

		}


		# if = {
		# 	limit = {
		# 		NOT = {
		# 			has_carrier_flag = bca_pf_fully_constructed
		# 		}
		# 	}
		# 	bca_resource_planet_controller = yes
		# }

	}
}


# trigger by on_district_complete
# remove district flag if construction completed
carrier_event = {
	id = colony_automation_event.11
	is_triggered_only = yes
	hide_window = yes
	trigger = {
		owner = {
			is_ai = no
		}
	}
	immediate = {
		remove_carrier_flag = bca_pf_plan_build_district_d0
		remove_carrier_flag = bca_pf_plan_build_district_d1
		remove_carrier_flag = bca_pf_plan_build_district_d2
		remove_carrier_flag = bca_pf_plan_build_district_d3
	}
}

# trigger by on_district_complete
# keep district plans compatible with the completed layout when demolition is disabled
carrier_event = {
	id = colony_automation_event.14
	is_triggered_only = yes
	hide_window = yes
	trigger = {
		owner = {
			is_ai = no
		}
		has_carrier_flag = bca_pf_disable_planet_auto_destruction_district
	}
	immediate = {
		bca_clamp_district_plan_to_current_layout = yes
	}
}



# trigger by on_building_complete
# remove building flag if construction completed
carrier_event = {
	id = colony_automation_event.13
	is_triggered_only = yes
	hide_window = yes
	trigger = {
		owner = {
			is_ai = no
		}
	}
	immediate = {
		remove_carrier_flag = bca_pf_has_building_build_plan
		remove_carrier_flag = bca_pf_plan_build_building_energy_grid
		remove_carrier_flag = bca_pf_plan_build_building_mineral_purification_plant
		remove_carrier_flag = bca_pf_plan_build_building_food_processing_facility
	}
}


# test event for debug, will be removed in final version
carrier_event = {
	id = colony_automation_event.501
	is_triggered_only = yes
	title = "flag 1"
	desc = "flag 1 set"
}

carrier_event = {
	id = colony_automation_event.502
	is_triggered_only = yes
	title = "flag 2"
	desc = "flag 2 set"
}

carrier_event = {
	id = colony_automation_event.503
	is_triggered_only = yes
	title = "503"
	desc = "503"
}

carrier_event = {
	id = colony_automation_event.504
	is_triggered_only = yes
	title = "504"
	desc = "504"
}

carrier_event = {
	id = colony_automation_event.505
	is_triggered_only = yes
	title = "505"
	desc = "505"
}

carrier_event = {
	id = colony_automation_event.506
	is_triggered_only = yes
	title = "506"
	desc = "506"
}
