automate_amenity_management = {
	available = {
		free_amenities < 0
		bca_has_minerals_to_build = yes
		owner = {
			NOR = {
				is_machine_empire = yes
				is_hive_empire = yes
			}
		}
	}

	emergency = yes
	category = "planet_automation_amenities"

	buildings = {
		clinic = {
			building = building_medical_2
			available = {
				owner = { has_country_flag = bca_build_medical_buildings }
				owner = { is_synthetic_empire = no }
				num_buildings = { type = building_medical_2 in_construction = yes value < 1 }
				NOT = { has_available_jobs = entertainer }
				NOT = { has_available_jobs = healthcare }
				NOT = { has_forbidden_jobs = entertainer }
				NOT = { has_forbidden_jobs = healthcare }
				any_owned_pop_group = { can_work_specific_job = healthcare }
			}

			upgrade_trigger = {
				owner = { has_country_flag = bca_build_medical_buildings }
				owner = { is_synthetic_empire = no }
				NOT = { has_available_jobs = entertainer }
				NOT = { has_available_jobs = healthcare }
				NOT = { has_forbidden_jobs = entertainer }
				NOT = { has_forbidden_jobs = healthcare }
				any_owned_pop_group = { can_work_specific_job = healthcare }
			}
		}

		holo = {
			building = building_holo_theatres
			available = {
				num_buildings = { type = building_holo_theatres in_construction = yes value < 1 }
				NOT = { has_available_jobs = entertainer }
				NOT = { has_available_jobs = healthcare }
				NOT = { has_forbidden_jobs = entertainer }
				NOT = { has_forbidden_jobs = healthcare }
				any_owned_pop_group = { can_work_specific_job = entertainer }
			}

			upgrade_trigger = {
				NOT = { has_available_jobs = entertainer }
				NOT = { has_available_jobs = healthcare }
				NOT = { has_forbidden_jobs = entertainer }
				NOT = { has_forbidden_jobs = healthcare }
				any_owned_pop_group = { can_work_specific_job = entertainer }
			}
		}
		zoo = {
			building = building_xeno_zoo
			available = {
				num_buildings = { type = building_xeno_zoo in_construction = yes value < 1 }
			}
		}
	}
}

automate_hive_amenity_management = {
	available = {
		free_amenities < 0
		bca_has_minerals_to_build = yes
		owner = {
			OR = {
				is_machine_empire = yes
				is_hive_empire = yes
			}
		}
		NOT = { has_available_jobs = maintenance_drone }
		NOT = { has_forbidden_jobs = maintenance_drone }
	}

	emergency = yes
	category = "planet_automation_amenities"

	buildings = {
		1 = {
			building = building_drone_storage
			available = {
				num_buildings = { type = building_drone_storage in_construction = yes value < 1 }
			}
		}
		2 = {
			building = building_hive_warren
			available = {
				num_buildings = { type = building_hive_warren in_construction = yes value < 1 }
			}
		}
	}
}
