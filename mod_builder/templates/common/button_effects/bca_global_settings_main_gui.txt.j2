bca_main_gui_open_global_settings = {
    potential = {
        exists = from
        from = { is_ai = no }
    }
    allow = {
        from = {
            NOT = {
                has_active_event = {
                    bca_global_settings_event.1
                }
            }
        }
    }
    effect = {
        hidden_effect = {
            from = {
                country_event = { id = bca_global_settings_event.1 }
            }
        }
    }
}
