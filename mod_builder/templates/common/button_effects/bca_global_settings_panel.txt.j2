bca_global_settings_default_auto_destruction_buildings_enable_current = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_building }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_BUILDING_AUTO_DEMOLITION_ON_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_buildings_enable = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_building
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_BUILDING_AUTO_DEMOLITION_ON_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                remove_country_flag = bca_default_disable_auto_destruction_building
            }
        }
    }
}

bca_global_settings_default_auto_destruction_buildings_disable_current = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_building
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_BUILDING_AUTO_DEMOLITION_OFF_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_buildings_disable = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_building }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_BUILDING_AUTO_DEMOLITION_OFF_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                set_country_flag = bca_default_disable_auto_destruction_building
            }
        }
    }
}

bca_global_settings_default_auto_destruction_districts_enable_current = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_district }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_DISTRICT_AUTO_DEMOLITION_ON_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_districts_enable = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_district
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_DISTRICT_AUTO_DEMOLITION_ON_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                remove_country_flag = bca_default_disable_auto_destruction_district
            }
        }
    }
}

bca_global_settings_default_auto_destruction_districts_disable_current = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_district
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_DISTRICT_AUTO_DEMOLITION_OFF_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_districts_disable = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_district }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_DISTRICT_AUTO_DEMOLITION_OFF_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                set_country_flag = bca_default_disable_auto_destruction_district
            }
        }
    }
}

bca_global_settings_default_auto_destruction_zones_enable_current = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_zone }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_ZONE_AUTO_DEMOLITION_ON_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_zones_enable = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_zone
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_ZONE_AUTO_DEMOLITION_ON_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                remove_country_flag = bca_default_disable_auto_destruction_zone
            }
        }
    }
}

bca_global_settings_default_auto_destruction_zones_disable_current = {
    potential = {
        exists = from
        from = {
            has_country_flag = bca_default_disable_auto_destruction_zone
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_ZONE_AUTO_DEMOLITION_OFF_CURRENT_TOOLTIP
    }
}

bca_global_settings_default_auto_destruction_zones_disable = {
    potential = {
        exists = from
        from = {
            NOT = { has_country_flag = bca_default_disable_auto_destruction_zone }
            check_variable = { which = bca_gui_active_tab value = 2 }
        }
    }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_DEFAULT_ZONE_AUTO_DEMOLITION_OFF_TARGET_TOOLTIP
        hidden_effect = {
            from = {
                set_country_flag = bca_default_disable_auto_destruction_zone
            }
        }
    }
}

bca_global_settings_auto_destruction_all_enable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_ALL_AUTO_DEMOLITION_ENABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.1 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_all_disable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_ALL_AUTO_DEMOLITION_DISABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.2 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_buildings_enable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_BUILDING_AUTO_DEMOLITION_ENABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.3 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_buildings_disable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_BUILDING_AUTO_DEMOLITION_DISABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.4 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_districts_enable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_DISTRICT_AUTO_DEMOLITION_ENABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.5 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_districts_disable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_DISTRICT_AUTO_DEMOLITION_DISABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.6 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_zones_enable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_ZONE_AUTO_DEMOLITION_ENABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.7 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

bca_global_settings_auto_destruction_zones_disable = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_EXISTING_PLANETS_ZONE_AUTO_DEMOLITION_DISABLE_TOOLTIP
        hidden_effect = {
            from = {
                country_event = { id = bca_auto_destruction_event.8 }
                bca_refresh_global_settings_panel_counts = yes
            }
        }
    }
}

{% for row in bca_global_settings_reserve_rows %}
    {% for step in row.negative_options + row.positive_options + row.legacy_options %}
        {% set tooltip_key = step.tooltip if step.tooltip is defined else row.tooltip %}
        {{ row.effect_prefix }}_{{ step.name }} = {
            potential = {
                exists = from
                from = {
                    {% if step.value < 0 %}
                        check_variable = { which = {{ row.variable }} value > 0 }
                    {% endif %}
                    check_variable = { which = bca_gui_active_tab value = 2 }
                }
            }
            allow = { always = yes }
            effect = {
                custom_tooltip = {{ tooltip_key }}
                hidden_effect = {
                    from = {
                        {% if step.value < 0 %}
                            if = {
                                limit = {
                                    check_variable = { which = {{ row.variable }} value <= {{ -step.value }} }
                                }
                                set_variable = { which = {{ row.variable }} value = 0 }
                            }
                            else = {
                                subtract_variable = { which = {{ row.variable }} value = {{ -step.value }} }
                            }
                        {% else %}
                            change_variable = { which = {{ row.variable }} value = {{ step.value }} }
                        {% endif %}
                    }
                }
            }
        }

    {% endfor %}
{% endfor %}

bca_global_settings_expect_output_rare_resource_decrease = {
    potential = { exists = from from = { check_variable = { which = bca_expect_output_rare_crystals value > 0 } } }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_RARE_RESOURCE_TARGET_SURPLUS_DECREASE_TOOLTIP
        hidden_effect = {
            from = {
                if = {
                    limit = { check_variable = { which = bca_expect_output_rare_crystals value <= 20 } }
                    set_variable = { which = bca_expect_output_rare_crystals value = 0 }
                    set_variable = { which = bca_expect_output_volatile_motes value = 0 }
                    set_variable = { which = bca_expect_output_exotic_gases value = 0 }
                }
                else = {
                    subtract_variable = { which = bca_expect_output_rare_crystals value = 20 }
                    subtract_variable = { which = bca_expect_output_volatile_motes value = 20 }
                    subtract_variable = { which = bca_expect_output_exotic_gases value = 20 }
                }
            }
        }
    }
}

bca_global_settings_expect_output_rare_resource_increase = {
    potential = { exists = from }
    allow = { always = yes }
    effect = {
        custom_tooltip = BCA_GLOBAL_SETTINGS_RARE_RESOURCE_TARGET_SURPLUS_INCREASE_TOOLTIP
        hidden_effect = {
            from = {
                change_variable = { which = bca_expect_output_rare_crystals value = 20 }
                change_variable = { which = bca_expect_output_volatile_motes value = 20 }
                change_variable = { which = bca_expect_output_exotic_gases value = 20 }
            }
        }
    }
}

bca_global_settings_retain_low_level_rare_resource_buildings_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bca_retain_low_level_rare_resource_buildings } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = bca_retain_low_level_rare_resource_buildings } } }
}

bca_global_settings_retain_low_level_rare_resource_buildings_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bca_retain_low_level_rare_resource_buildings } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = bca_retain_low_level_rare_resource_buildings } } }
}

bca_global_settings_remove_low_level_rare_resource_buildings_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bca_retain_low_level_rare_resource_buildings } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = bca_retain_low_level_rare_resource_buildings } } }
}

bca_global_settings_remove_low_level_rare_resource_buildings_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bca_retain_low_level_rare_resource_buildings } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = bca_retain_low_level_rare_resource_buildings } } }
}

bca_global_settings_pop_assemble_always_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bca_build_pop_assemble_even_if_no_jobs } }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                set_country_flag = bca_build_pop_assemble_even_if_no_jobs
                remove_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
}

bca_global_settings_pop_assemble_always_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bca_build_pop_assemble_even_if_no_jobs } } }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                set_country_flag = bca_build_pop_assemble_even_if_no_jobs
                remove_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
}

bca_global_settings_pop_assemble_needed_selected = {
    potential = {
        exists = from
        from = {
            check_variable = { which = bca_gui_active_tab value = 2 }
            NOR = {
                has_country_flag = bca_build_pop_assemble_even_if_no_jobs
                has_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                remove_country_flag = bca_build_pop_assemble_even_if_no_jobs
                remove_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
}

bca_global_settings_pop_assemble_needed_unselected = {
    potential = {
        exists = from
        from = {
            check_variable = { which = bca_gui_active_tab value = 2 }
            OR = {
                has_country_flag = bca_build_pop_assemble_even_if_no_jobs
                has_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                remove_country_flag = bca_build_pop_assemble_even_if_no_jobs
                remove_country_flag = bac_remove_pop_assemble_buildings
            }
        }
    }
}

bca_global_settings_pop_assemble_demolish_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bac_remove_pop_assemble_buildings } }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                set_country_flag = bac_remove_pop_assemble_buildings
                remove_country_flag = bca_build_pop_assemble_even_if_no_jobs
            }
        }
    }
}

bca_global_settings_pop_assemble_demolish_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bac_remove_pop_assemble_buildings } } }
    allow = { always = yes }
    effect = {
        hidden_effect = {
            from = {
                set_country_flag = bac_remove_pop_assemble_buildings
                remove_country_flag = bca_build_pop_assemble_even_if_no_jobs
            }
        }
    }
}

{% set building_policy_settings = [
    {"name": "biological_pop_assembly_buildings", "build_flag": "bca_build_biological_pop_assembly_buildings", "remove_flag": "bca_remove_biological_pop_assembly_buildings"},
    {"name": "mechanical_pop_assembly_buildings", "build_flag": "bca_build_mechanical_pop_assembly_buildings", "remove_flag": "bca_remove_mechanical_pop_assembly_buildings"},
    {"name": "medical_buildings", "build_flag": "bca_build_medical_buildings", "remove_flag": "bca_remove_medical_buildings"}
] %}
{% for setting in building_policy_settings %}
bca_global_settings_{{ setting.name }}_build_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } has_country_flag = {{ setting.build_flag }} } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = {{ setting.build_flag }} remove_country_flag = {{ setting.remove_flag }} } } }
}

bca_global_settings_{{ setting.name }}_build_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } NOT = { has_country_flag = {{ setting.build_flag }} } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = {{ setting.build_flag }} remove_country_flag = {{ setting.remove_flag }} } } }
}

bca_global_settings_{{ setting.name }}_retain_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } NOR = { has_country_flag = {{ setting.build_flag }} has_country_flag = {{ setting.remove_flag }} } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = {{ setting.build_flag }} remove_country_flag = {{ setting.remove_flag }} } } }
}

bca_global_settings_{{ setting.name }}_retain_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } OR = { has_country_flag = {{ setting.build_flag }} has_country_flag = {{ setting.remove_flag }} } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = {{ setting.build_flag }} remove_country_flag = {{ setting.remove_flag }} } } }
}

bca_global_settings_{{ setting.name }}_demolish_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } has_country_flag = {{ setting.remove_flag }} } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = {{ setting.remove_flag }} remove_country_flag = {{ setting.build_flag }} } } }
}

bca_global_settings_{{ setting.name }}_demolish_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 } NOT = { has_country_flag = {{ setting.remove_flag }} } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = {{ setting.remove_flag }} remove_country_flag = {{ setting.build_flag }} } } }
}

{% endfor %}

bca_global_settings_deurbanization_off_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bac_allow_deurbanization } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = bac_allow_deurbanization } } }
}

bca_global_settings_deurbanization_off_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bac_allow_deurbanization } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { remove_country_flag = bac_allow_deurbanization } } }
}

bca_global_settings_deurbanization_on_selected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  has_country_flag = bac_allow_deurbanization } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = bac_allow_deurbanization } } }
}

bca_global_settings_deurbanization_on_unselected = {
    potential = { exists = from from = { check_variable = { which = bca_gui_active_tab value = 2 }  NOT = { has_country_flag = bac_allow_deurbanization } } }
    allow = { always = yes }
    effect = { hidden_effect = { from = { set_country_flag = bac_allow_deurbanization } } }
}
